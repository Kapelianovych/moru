{
  "name": "moru-tests",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "npm run test:server && npm run test:browser",
    "test:server": "vitest run --config ./vite.server.config.js",
    "test:browser": "vitest run --config ./vite.browser.config.js"
  },
  "dependencies": {
    "moru": "*"
  },
  "devDependencies": {
    "jsdom": "^20.0.3",
    "vite": "^3.2.4",
    "vitest": "^0.25.2"
  }
}
